# `bun run generate` drives the Claude Code CLI (`claude -p`) using your local
# Claude Max subscription login — no API key required. Just make sure you're
# logged in: run `claude` once interactively and authenticate.
#
# Kept (empty of secrets) so the repo documents that generation needs the
# `claude` CLI on PATH and an active Max login — not an ANTHROPIC_API_KEY.
